{"id":"state-transition","version":"1.0.0","phase":"cutover","hazard":"Database initialization or privilege separation differs from the admitted reset-to-empty transition.","evidence":"Two immutable schema-runner outcomes, schema checksum/version/empty predicates and exact runtime-grant digest","predicate":"A generated ephemeral migration identity runs apply-schema.js twice (apply then idempotence) and is removed; runtime receives SELECT on schema_versions and SELECT/INSERT/UPDATE on projects only; the service starts disabled behind the fence.","timeout_seconds":45,"max_attempts":1,"failure_action":"rollback","secret_policy":"names-and-digests-only"}
