{"id":"serving-topology","version":"1.0.0","phase":"cutover","hazard":"The wrong process, listener, group boundary or nginx route serves PRD Studio.","evidence":"Protected regular-file captures of systemd cgroup process IDs, MainPID status and socket ownership plus nginx validation","predicate":"Exactly one process in the service cgroup is MainPID and owns the 0660 Unix socket; a dedicated socket group connects nginx without exposing app or proxy credentials; the atomically patched active nginx parents validate.","timeout_seconds":25,"max_attempts":1,"failure_action":"rollback","secret_policy":"names-and-digests-only"}
