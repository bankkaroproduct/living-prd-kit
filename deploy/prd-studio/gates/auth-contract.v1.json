{"id":"auth-contract","version":"1.0.0","phase":"cutover","hazard":"Basic Auth or the trusted-proxy boundary can be bypassed or valid traffic is rejected.","evidence":"Direct Unix-socket marker probes plus public no-credential, spoofed-marker, wrong-credential and valid-credential statuses","predicate":"The backend rejects a missing proxy marker; the public edge strips a spoofed marker, challenges missing/wrong credentials, and accepts only the private certified credential.","timeout_seconds":25,"max_attempts":1,"failure_action":"rollback","secret_policy":"names-and-digests-only"}
